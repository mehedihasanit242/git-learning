git learning 

…or create a new repository on the command line
echo "# git-learning" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/mehedihasanit242/git-learning.git
git push -u origin main
…or push an existing repository from the command line
git remote add origin https://github.com/mehedihasanit242/git-learning.git
git branch -M main
git push -u origin main
…or import code from another repository
You can initialize this repository with code from a Subversion, Mercurial, or TFS project.



first desctop folder add git repository command---- git init
2.then folder create command mkdir, file crate command

cd desktop
cd.. 
mkdir
touch file name
git status 
git log
git add .
git diff
git commit -m "comment"
git checkout (version name)
git show
git diff --staged
